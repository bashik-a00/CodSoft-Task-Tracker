/*import { useState } from "react";
import TaskForm from "./TaskForm";
const TaskWrapper=()=>{
  const [tasks,setTasks]=useState([]);

  const addTask=(todo)=>{
    setTasks([...tasks,{task:todo,completed:false}]);
    console.log(tasks);
  }

  return(
    <div>
      <TaskForm addTask={addTask}/>
    </div>
  );
}

export default TaskWrapper;*/
